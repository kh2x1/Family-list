# إعدادات النشر على Netlify
[build]
  command = "npm run build"
  publish = "dist"

# توجيه كل المسارات إلى index.html (تطبيق صفحة واحدة)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
